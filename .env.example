# Exemplo de variáveis de ambiente necessárias para rodar LegacyGuard
# Não comite suas chaves reais.

# OpenAI
OPENAI_API_KEY=sk-...

# NextAuth
NEXTAUTH_SECRET=uma-secret-chave
NEXTAUTH_URL=https://seu-projeto.vercel.app
GITHUB_ID=seu-github-client-id
GITHUB_SECRET=seu-github-client-secret

# Redis (fila e streams)
REDIS_URL=redis://:senha@seu-redis-host:6379

# Postgres / Audit / RAG
AUDIT_DB_URL=postgres://user:pass@host:5432/legacyguard
# Alternativa: conexão para um DB com pgvector (use apenas uma das duas URLs conforme sua infra)
PGVECTOR_URL=postgres://user:pass@host:5432/legacyguard

# Sandbox (opcional)
LEGACYGUARD_SANDBOX_ENABLED=true
LEGACYGUARD_SANDBOX_REPO_PATH=/workspace/legacyguard
LEGACYGUARD_SANDBOX_COMMAND=npm test
LEGACYGUARD_SANDBOX_RUNNER=/workspace/legacyguard/scripts/runner_sandbox.sh
LEGACYGUARD_SANDBOX_TIMEOUT_MS=900000

# Modelos (opcional)
OPENAI_CHEAP_MODEL=gpt-4o-mini
OPENAI_DEEP_MODEL=gpt-4o
